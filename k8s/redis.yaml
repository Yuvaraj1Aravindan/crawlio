apiVersion: apps/v1
kind: Deployment
metadata:
  name: crawler-redis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: crawler-redis
  template:
    metadata:
      labels:
        app: crawler-redis
    spec:
      containers:
      - name: redis
        image: redis:7-alpine
        ports:
        - containerPort: 6379
---
apiVersion: v1
kind: Service
metadata:
  name: crawler-redis
spec:
  selector:
    app: crawler-redis
  ports:
  - port: 6379
    targetPort: 6379
